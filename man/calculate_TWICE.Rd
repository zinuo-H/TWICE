% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TWICE.R
\name{calculate_TWICE}
\alias{calculate_TWICE}
\title{Calculate TWiCE Scores for Pathways}
\usage{
calculate_TWICE(
  all_NS_res,
  cor_df,
  mode = c("directed", "mixed", "separate")[3],
  universe = cor_df$name,
  p.adjust.method = "BH",
  spearate_res = FALSE,
  perm = 999,
  parallel = TRUE,
  cores = 8,
  min_exist_KO = 1,
  max_exist_KO = 600
)
}
\arguments{
\item{all_NS_res}{A data frame containing pathway information with columns:
Pathway_id, name, and NS.}

\item{cor_df}{A data frame containing correlation data with columns: name and cor.}

\item{mode}{Character string specifying the mode. One of 'mixed', 'directed', or 'separate'.
Default is 'separate'.}

\item{universe}{Character vector of KO names to consider as the background universe.
Default is cor_df$name.}

\item{p.adjust.method}{Character string specifying the method for p-value adjustment.
Default is "BH".}

\item{spearate_res}{Logical. When mode is 'separate', determines whether to return
a combined data frame with positive and negative results stacked (FALSE) or a single
wide data frame (TRUE). Default is FALSE.}

\item{perm}{Integer. Number of permutations for null distribution generation.
Default is 999.}

\item{parallel}{Logical. Whether to use parallel processing. Default is TRUE.}

\item{cores}{Integer. Number of CPU cores to use when parallel = TRUE. Default is 8.}

\item{min_exist_KO}{Integer. Minimum number of KOs in a pathway that must have
corresponding correlation values to compute scores. Default is 1.}

\item{max_exist_KO}{Integer. Maximum number of KOs in a pathway that can have
corresponding correlation values to compute scores. Default is 600.}
}
\value{
A data frame with columns:
\itemize{
\item Pathway_id: The ID of the pathway.
\item K_num: The total number of KOs in the pathway.
\item Exist_K_num: The number of KOs in the pathway that have corresponding correlation.
\item Exist_K: The names of the KOs in the pathway that have corresponding correlation,
separated by "|".
\item S_mean: The mean TWiCE score for the pathway.
\item S_sum: The sum TWiCE score for the pathway (S_mean multiplied by Exist_K_num).
\item p_value: The p-value for the pathway based on permutation testing.
\item p_adjust: The adjusted p-value for the pathway.
\item Direction: (only for mode = 'separate' and spearate_res = FALSE) Indicates whether
the result is for positive or negative correlations ("Positive" or "Negative").
}
}
\description{
This function calculates TWiCE scores, p-values, and secondary p-values for each pathway
based on correlation data and NS values. It supports three modes: 'mixed', 'directed', and 'separate'.
}
\seealso{
calculate_NS
}
